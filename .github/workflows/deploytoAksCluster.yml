on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    
    - uses: azure/docker-login@v1
      with:
        login-server: epmdb9918.azurecr.io
        username: ${{ secrets.acr_epmdb9918_username }}
        password: ${{ secrets.acr_epmdb9918_password }}
    
    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/app" -f  "app/Dockerfile" -t epmdb9918.azurecr.io/aks01:${{ github.sha }} --label dockerfile-path=app/Dockerfile
        docker push epmdb9918.azurecr.io/aks01:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      with:
         kubeconfig: ${{ secrets.aks_aks01_kubeConfig }}
      id: login
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep aks01b251 | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace aks01b251`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: aks01b251
        container-registry-url: epmdb9918.azurecr.io
        container-registry-username: ${{ secrets.acr_epmdb9918_username }}
        container-registry-password: ${{ secrets.acr_epmdb9918_password }}
        secret-name: aks01dockerauth
       
    - uses: azure/k8s-deploy@v1.2
      with:
        namespace: aks01b251
        manifests: |
          manifests/deployment-3.yml
          manifests/service-3.yml
        images: |
          epmdb9918.azurecr.io/aks01:${{ github.sha }}
        imagepullsecrets: |
          aks01dockerauth
